import React from 'react';
import styled from '@emotion/styled';

const SearchBlock = styled.div``;

interface SearchProps {}

function Search(props: SearchProps) {
  return <SearchBlock>Search</SearchBlock>;
}

export default Search;
