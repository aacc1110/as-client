import React from 'react';
import styled from 'styled-components';

interface MenuProps {}

const MenuBlock = styled.div``;

function Menu(props: MenuProps) {
  return <MenuBlock>MENU</MenuBlock>;
}

export default Menu;
